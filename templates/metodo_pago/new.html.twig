

{% extends 'base2.html.twig' %}

{% block title %}Crear un Método de Pago{% endblock %}

{% block body %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <button type="button" id="sidebarCollapse" class="btn btn-dark mr-2">
            <i class="fas fa-bars"></i>
        </button>
        <a class="navbar-brand" href="#">
            <img src="../{{ logo }}" alt="Logo">
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="#"><i class="fas fa-user-circle mr-1"></i> Perfil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#"><i class="fas fa-cog mr-1"></i> Configuración</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#"><i class="fas fa-sign-out-alt mr-1"></i> Salir</a>
                </li>
            </ul>
        </div>
    </nav>
    <!-- Sidebar -->
    {% include "bar_nav_dashboard_admin.html.twig" %}

    <!-- Contenido Principal -->
    <div id="content" style="padding:20px !important">
        <div class="row" style="padding-top: 20px !important">
            <div class="col-12 col-md-8 col-lg-8" style="margin:auto !important" >
                &nbsp;          
            </div>
        </div>    
        <div class="row">
            <div class="col-12 col-md-8 col-lg-8" style="margin:auto !important" >
                <div class="text-right"><a href="{{ path('app_metodo_pago_index') }}" title="Regresar al listado"><i class="fas fa-list-ol"></i></a></div>
                <div class="card shadow">
                    <div class="card-header text-center"><h4>Crea una nuevo Método de Pago</h4></div>
                    <div class="card-body">                
                        {{ include('metodo_pago/_form.html.twig') }}
                    </div>
                </div>
                <div class="text-right"><a href="{{ path('app_metodo_pago_index') }}" title="Regresar al listado"><i class="fas fa-list-ol"></i></a></div>
            </div>
        </div>

    </div>
{% endblock %}
